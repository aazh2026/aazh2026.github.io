name: Deploy Jekyll Blog to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.2'
          bundler-cache: true
          cache-version: 0

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install npm deps (svgo for SVG verification)
        run: npm ci

      - name: Verify SVGs are svgo-optimized
        run: node scripts/check-svgo.js

      - name: Setup Pages
        id: pages
        uses: actions/configure-pages@v6
        
      - name: Build site
        run: bundle exec jekyll build --baseurl "${{ steps.pages.outputs.base_path }}"
        env:
          JEKYLL_ENV: production

      # PageFind builds a static search index over the built site, written to
      # _site/pagefind/. Must run after Jekyll so it sees the final HTML, and
      # before upload-pages-artifact so the chunks ship with the deploy.
      - name: Build search index (PageFind)
        run: npx pagefind --site _site

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v5

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-24.04
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
